# pixel_settings.hpp

**路径**: `GL/glutils/pixel_settings.hpp`  
**类型**: 头文件  
**大小**: 3366 字节

## 文件描述

该文件是 RPCS3 RSX 模块的一部分，负责**OpenGL 图形 API 后端实现**。

## 主要组件

### 类/结构体

- **pixel_pack_settings**
- **pixel_unpack_settings**

## 主要函数

- `image_height()`
- `get_row_length()`
- `get_swap_bytes()`
- `alignment()`
- `skip_rows()`
- `skip_images()`
- `lsb_first()`
- `row_length()`
- `swap_bytes()`
- `skip_pixels()`
- `apply()`

## 依赖关系

### 包含的头文件

```cpp
#include <common.h>
```

### 命名空间

- `gl`

## 代码统计

- 总行数: 157
- 类/结构体数量: 2
- 函数数量: 11
- 枚举数量: 0

## 相关文件

*无直接关联文件*

