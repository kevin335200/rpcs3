# tuple.hpp

## 文件信息
- **路径**: /home/user/rpcs3/rpcs3/util/tuple.hpp
- **类型**: 头文件

## 功能概述
实现轻量级tuple类型，约束为平凡可复制类型，用于编译期计算。

## 主要内容

### 核心特性
- 继承链式结构存储多个元素
- 支持编译期约束：所有元素必须是平凡可复制且平凡可析构
- `get<N>()`: 编译期索引访问

## 学习要点
- 递归继承模式
- `if constexpr` 用于编译期递归展开
- Concepts约束确保类型安全
